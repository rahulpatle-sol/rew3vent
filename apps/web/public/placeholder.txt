This is a placeholder file for the public directory.
Static assets like images, fonts (not managed by next/font), and other files can be placed here.
They will be served from the root of your application (e.g., /my-image.png if my-image.png is in public/).
